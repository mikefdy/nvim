### Setup

1. Go into your config folder, or where ever you want to have the nvim config.
`cd ~/.config`

2. Clone the repo
`git clone git@github.com:mikefdy/nvim.git`

Your installation should appear as 
`.../.config/nvim`
